<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace속성:매퍼파일의 완전한경로 .xml는 생략 -->
<!-- ※ibatis와는 다르게 id값에 .(dot)를 사용 못한다. -->
<mapper namespace="mybatis.Product_mybatis.xml">
	<!--resultMap사용시 column속성 필수  -->
	
 
 	
	<!-- private String nt_no;//키값
	private java.util.Date nt_regidate;//날짜
	private String ad_no;//관리자 키값
	private String nt_classification;//분류(서비스소식,종료,오픈등등)
	private String nt_title;//제목
	private String nt_contents;//내용
	private String nt_hitcount;//조회수 -->
	
	
	
 	<!-- id속성에 지정한 값을 자바코드(DAO계열 클래스)에서 
           식별자로 사용 -->
           
  <select id="ProductList" parameterType="java.util.Map" resultType="Product">
   SELECT *  FROM PD_PRODUCT ORDER BY PD_REGIDATE DESC
	</select> 
 <select id="ProductView" parameterType="java.util.Map" resultType="Product">
 SELECT * FROM PD_PRODUCT WHERE PD_NO=#{pd_no}
		
	</select>   
	
 	
</mapper>